using TaleWorlds.SaveSystem;

namespace CampaignPacer
{
    /*
    public class CustomSaveableTypeDefiner : SaveableTypeDefiner
    {
        public const int zijistark0_SaveBaseId = 19841119; // zijistark's 0th allocated range is 1984111900 through 1984111999
        public const int SaveBaseId = zijistark0_SaveBaseId + 0;

        public CustomSaveableTypeDefiner() : base(SaveBaseId) { }

        protected override void DefineClassTypes()
        {
            AddClassDefinition(typeof(Main), 1);
            AddClassDefinition(typeof(SimpleTime), 2);
        }
    }
    */
}
